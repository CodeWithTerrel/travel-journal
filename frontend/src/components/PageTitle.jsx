export default function PageTitle({ children }) {
    return (
        <div className="inline-block bg-[#0F766E] text-white text-2xl px-6 py-2 rounded-full shadow-sm">
            {children}
        </div>
    );
}
